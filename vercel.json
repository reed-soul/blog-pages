{
  "build": {
    "env": {
      "HUGO_VERSION": "0.123.6"
    }
  },
  "cleanUrls": true,
  "buildCommand": "curl -L -o go.tar.gz https://go.dev/dl/go1.23.4.linux-amd64.tar.gz && tar -C /tmp -xzf go.tar.gz && export PATH=$PATH:/tmp/go/bin && hugo --gc --minify"
} 